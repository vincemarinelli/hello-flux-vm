# Use a minimal Linux base image, e.g., Alpine Linux
FROM alpine:latest

# Set the default command to run when the container starts
# This will execute a shell, providing a command-line interface
CMD ["sh"]